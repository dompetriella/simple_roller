import 'package:flutter/material.dart';

class DiceButton extends StatelessWidget {
  const DiceButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
